<header class="header">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <h1 class="site_logo">
      <a href="/" class="navbar-brand">
        <!--<img src="/assets/site-logo.png" alt="ながのCAKE">-->
        LOGO
      </a>
    </h1>
    <% if admin_signed_in? %>
      <p class="text-light">you are admin now.</p>
    <% end %>
    <% if customer_signed_in? %>
      <p class="text-light"><%= current_customer.id %>さん</p>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ml-auto">
        <li><%= link_to 'ranks', ranks_path, class: 'fas fa-cookie nav-link text-light' %></li>
        <li><%= link_to 'new_rank', new_rank_path, class: 'fas fa-user nav-link text-light' %></li>
        <li><%= link_to 'adminログアウト', destroy_admin_session_path, method: :delete, class: 'fas fa-sign-out-alt nav-link text-light' %></li>
      </ul>
    </div>
  </nav>
</header>