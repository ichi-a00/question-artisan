<h1>Customer::Homes#top</h1>
<p>Find me in app/views/customer/homes/top.html.erb</p>

<% if @tags.present? %>
  <% @tags.each do |tag| %>
    <%= link_to "#{tag.name}(#{tag.taggings_count})", tags_path(tag.name) %>
  <% end %>
<% else %>
  <p>登録されているタグはありません</p>
<% end %>